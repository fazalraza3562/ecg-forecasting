# Overrides default.yaml: bumped epochs (small transformers converge slower), lower peak LR,
# ~10% linear warmup, longer early-stop patience.
epochs: 40
learning_rate: 1.0e-4
warmup_epochs: 3
early_stopping_patience: 15
